[project]
name = "local-db-mcp-server-202607"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.28.1",
    "psycopg[binary]>=3.2.0",
]
